name: alex_mobile
description: "Alex — assistant personnel intelligent."
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # État & réseau
  provider: ^6.1.2
  http: ^1.2.2
  shared_preferences: ^2.3.2
  uuid: ^4.5.0

  # Rendu & typographie
  google_fonts: ^6.2.1
  flutter_markdown_plus: ^1.0.6

  # Carte
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # WebSocket
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
